# REAL-Edu 운영 관리 설계

## 현재 구현

`dist/admin.html`에서 운영 UI를 확인한다. 샘플 데이터만 포함하며 인증된 관리자 서비스가 아니다. 로컬 변경은 `realEduAdminDraftV1` localStorage 키에 저장된다. 공개 사이트, 실제 회원, 결제, 메일 발송에는 영향을 주지 않는다. 실회원 개인정보를 이 화면에 입력하면 안 된다. 로컬 변경 이력은 최근 100건만 남으며 신뢰 가능한 서버 감사 로그를 대체하지 않는다.

- 대시보드: 등록 강좌, 예시 결제 합계, 예시 회원, 미처리 문의, 환불 요청, 캠페인 일정.
- 강좌: 제목, 분류, 수강료, 정원, 일정, 이미지, 모집 상태와 메모. 신규 강좌는 관리 초안으로만 추가되며 상세 페이지가 자동 발행되지 않는다.
- 이벤트: 기간, 상태, 연결 강좌와 운영 메모.
- 배너: 이미지, 제목, 설명, 버튼 문구·주소, 노출 위치·대상·빈도·기간·우선순위, 미리보기. 예약 상태는 실제 스케줄러 동작을 의미하지 않는다.
- 회원: 조회·검색, 예시 상태 수정, 수강·문의 이력, 내부 메모. 실제 제재나 권한 변경은 발생하지 않는다.
- 주문: 금액·결제 상태는 조회 전용. 검토 상태와 메모만 편집한다. 실제 환불 실행 기능은 포함하지 않는다.
- 문의: 상태, 답변 초안과 내부 메모. 실제 이메일 발송 기능은 포함하지 않는다.
- 설정: 브랜드·사업자 정보 초안, 연결 현황, 역할 설계.
- 변경 이력 / JSON 내보내기: 로컬 운영 초안 보관.

## 실서비스 연결 순서

1. 관리자 인증과 서버 권한 검사: 별도 인증 서비스, MFA, 세션 만료, 역할별 접근. 관리자 HTML 주소를 숨기는 것은 보안 수단이 아니다. 현재 GitHub Pages만으로 안전한 회원 관리 API를 제공할 수 없다.
2. 데이터베이스와 API: courses, course_sessions, members, enrollments, orders, payments, refunds, inquiries, campaigns, banners, assets, admin_users, audit_logs. 개인 정보는 공개 정적 파일에 포함하지 않는다.
3. 공개 콘텐츠 API: 초안과 게시본 분리, 게시 승인, 예약 실행, 미리보기 토큰, 버전 복구. 홈페이지는 게시본만 읽는다. 배너 노출은 서버 시간·Asia/Seoul 기준, 시작 포함·종료 제외로 정의하고 대상·빈도 정책을 구현한다.
4. 결제 연결: 서버에서 가격·정원 검증, 주문 생성, 결제사 웹훅 서명 검증 및 멱등 처리, 상태 전이 기록. 환불은 별도 권한·승인과 결제사 성공 응답 후 반영한다.
5. 문의와 운영 알림: 수신 DB, 답변 발송 큐, 실패 재시도, 배송/발송 이력, 담당자 할당. 내부 메모와 고객 발송 내용을 분리한다.
6. 파일 저장소: 이미지 업로드 유형·크기 검증, 썸네일 변환, 접근 제어, 참조 중 파일 삭제 방지.
7. 운영 안정화: 백업·복구, 감사 로그, 민감정보 마스킹, 필드별 접근 제어, 삭제·보관 정책, 모니터링.

## 권한 제안

| 역할 | 허용 범위 |
| --- | --- |
| 최고 관리자 | 운영 설정, 관리자 초대·권한, 전체 콘텐츠 승인 |
| 콘텐츠 매니저 | 강좌·기수·일정·배너·이벤트 초안 작성 |
| 고객지원 | 필요한 회원 정보와 수강 이력 조회, 문의 처리, 환불 검토 |
| 재무 담당 | 주문·정산 조회, 승인된 환불 실행 |

모든 역할은 서버에서 검사한다. 사용자 화면의 숨김·비활성화는 권한 검사를 대체하지 않는다. 회원정보 내보내기는 목적·권한·기록을 갖춘 별도 기능으로 설계한다.

## 다음 확장 후보

- 강좌 기수별 강사·강의실·정원, 출석, 자료 배포, 수료증.
- 쿠폰·프로모션 조건, 중복 할인 제한, 유효기간과 사용 이력.
- 매출·환불·정산 리포트와 강좌별 전환 분석. 실데이터 연결 전 수치를 성과로 표시하지 않는다.
- 공지·FAQ·정책 버전 관리와 약관 동의 이력.
- 담당자 배정, 처리 기한, 알림센터, 운영 작업 캘린더.

## 수동 검증

관리 메뉴 전환, 검색·상태 필터·목록 페이지 이동, 강좌 편집과 새로고침 후 유지, 배너 날짜 역전 거부·필수 일정 검사, 배너 미리보기, 문의 초안 저장, 주문의 결제 상태 조회 전용 여부를 확인한다.
